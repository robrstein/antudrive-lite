import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AntuDrive Lite smoke test', (WidgetTester tester) async {
    // Placeholder — app requires BLE and SharedPreferences; tested on device.
    expect(true, isTrue);
  });
}
